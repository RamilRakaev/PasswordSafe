<Window x:Class="PasswordSafe.WPF.Views.CategoryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Manage Categories" Height="400" Width="500"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <DataGrid x:Name="CategoriesGrid" 
                  Grid.Row="0"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  SelectionMode="Single"
                  Margin="0,0,0,10">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" IsReadOnly="True" Width="50"/>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBox x:Name="NewCategoryBox" Width="300" Margin="0,0,10,0" 
                     Text="New category name"/>
            <Button Content="Add" Click="Add_Click" Width="80" Margin="0,0,5,0"/>
            <Button Content="Update" Click="Update_Click" Width="80" Margin="0,0,5,0"/>
            <Button Content="Delete" Click="Delete_Click" Width="80"/>
        </StackPanel>

        <Button Grid.Row="2" Content="Close" Click="Close_Click" 
                Width="100" HorizontalAlignment="Right"/>
    </Grid>
</Window>
